
import { EventsList } from "@/components/dashboard/events-list"

export const metadata = {
  title: "Events | Dashboard",
  description: "Manage your portfolio events",
}

export default function EventsPage() {
  return <EventsList />
}
